package ai.privado.auth

import org.scalatest.BeforeAndAfterAll
import org.scalatest.matchers.should.Matchers
import org.scalatest.wordspec.AnyWordSpec

class AuthHandlerTest extends AnyWordSpec with Matchers with BeforeAndAfterAll {

  "upload flag with consent" should {
//    val authHandler = spy(AuthenticationHandler)
    // TODO: refactor the code add unit tests
//    when(authHandler.syncToCloud).thenReturn()
  }
}
